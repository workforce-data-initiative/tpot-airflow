version: '3.2'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./dags:/usr/src/app/dags
      - ./logs:/usr/src/app/logs
    ports:
      - 8080:8080
